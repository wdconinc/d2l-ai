# container_apps module

Creates an Azure Container Apps environment and a placeholder app to expose an endpoint output.
Ingress exposure is configurable via `external_enabled` and defaults to internal-only.
