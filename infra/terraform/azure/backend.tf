# Remote-state scaffold (Azure Storage backend).
# Copy backend.hcl.example to backend.hcl and fill real values, then run:
# terraform init -backend-config=backend.hcl
#
# terraform {
#   backend "azurerm" {}
# }
